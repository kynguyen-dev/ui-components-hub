---
title: "Magic UI"
description: "Animated, high-impact components for landing pages, marketing surfaces, and premium user experiences."
library: "Magic UI"
---

import { ComponentPreview } from '@/components/mdx/ComponentPreview';
import { MagicMarqueePreview } from '@/components/mdx/MagicMarqueePreview';
import {
  MagicAnimatedGradientTextPreview,
  MagicAnimatedShinyTextPreview,
  MagicAuroraTextPreview,
  MagicHyperTextPreview,
  MagicLineShadowTextPreview,
  MagicMorphingTextPreview,
  MagicNumberTickerPreview,
  MagicScrollBasedVelocityPreview,
  MagicSparklesTextPreview,
  MagicSpinningTextPreview,
  MagicText3DFlipPreview,
  MagicTextAnimatePreview,
  MagicTextHighlighterPreview,
  MagicTextRevealPreview,
  MagicTypingAnimationPreview,
  MagicVideoTextPreview,
  MagicWordRotatePreview,
} from '@/components/mdx/MagicTextAnimationsShowcase';
import { ShimmerButton } from '@/components/ui/shimmer-button';
import { RainbowButton } from '@/components/ui/rainbow-button';
import { RippleButton } from '@/components/ui/ripple-button';
import { ShinyButton } from '@/components/ui/shiny-button';
import { PulsatingButton } from '@/components/ui/pulsating-button';
import { InteractiveHoverButton } from '@/components/ui/interactive-hover-button';
import { Safari } from '@/components/ui/safari';
import { Iphone } from '@/components/ui/iphone';
import { Android } from '@/components/ui/android';
import { FlickeringGrid } from '@/components/ui/flickering-grid';
import { NeonGradientCard } from '@/components/ui/neon-gradient-card';
import { RetroGrid as MagicUiRetroGrid } from '@/components/ui/retro-grid';
import {
  AnimatedGridPattern,
  Ripple,
  DotPattern,
  GridPattern,
  HexagonPattern,
  StripedPattern,
  InteractiveGridPattern,
  LightRays,
  NoiseTexture,
} from '@/components/ui/magic-backgrounds';

## Overview

**Magic UI** is a collection of animated React components designed for marketing and landing page surfaces. Where shadcn/ui focuses on functional UI primitives, Magic UI focuses on **visual impact** — shimmer effects, particle animations, gradient flows, and text reveals.

## Available Components

| Component | Category | Status | Description |
| :--- | :--- | :--- | :--- |
| **Buttons** | Actions | Stable | Premium animated button patterns including shimmer, rainbow, ripple, shiny, pulsating, and interactive hover styles |
| **Marquee** | Layout Motion | Stable | Infinite scrolling rows for testimonials, logos, announcements, and media strips |
| **Device Mocks** | Showcase | Stable | Browser and mobile device mockups for screenshots, demos, and launch visuals |
| **Backgrounds** | Decorative | New | Animated grid, pattern, ripple, and texture effects for hero sections and ambient page framing |
| **Special Effects** | Accent | New | High-impact visual treatments like neon cards, beams, particles, and animated emphasis layers |
| **Text Animations** | Typography | Stable | Animated text treatments for hero copy, storytelling sections, stats, and expressive headlines |

## Buttons

### Rainbow Button

<ComponentPreview
  client:load
  description="Gradient bordered buttons in default and outline variants."
  code={`import { RainbowButton } from '@/components/ui/rainbow-button';

export function Demo() {
  return (
    <div className="flex gap-4">
      <RainbowButton>Default Variant</RainbowButton>
      <RainbowButton variant="outline">Outline Variant</RainbowButton>
    </div>
  );
}`}
>
  <div className="flex flex-wrap gap-4 items-center justify-center">
    <RainbowButton client:load>Default Variant</RainbowButton>
    <RainbowButton client:load variant="outline">Outline Variant</RainbowButton>
  </div>
</ComponentPreview>

### Shiny Button

<ComponentPreview
  client:load
  description="An elegant, modern button with a smooth animated shine effect."
  code={`import { ShinyButton } from '@/components/ui/shiny-button';

export function Demo() {
  return <ShinyButton>Pro Upgrade</ShinyButton>;
}`}
>
  <ShinyButton client:load>Pro Upgrade</ShinyButton>
</ComponentPreview>

### Shimmer Button

<ComponentPreview
  client:load
  description="A gradient button with a rotating conic-gradient shimmer effect."
  code={`import { ShimmerButton } from '@/components/ui/shimmer-button';

export function Demo() {
  return <ShimmerButton>Start Building →</ShimmerButton>;
}`}
>
  <ShimmerButton client:load className="px-8 py-4 text-base">Start Building →</ShimmerButton>
</ComponentPreview>

### Pulsating Button

<ComponentPreview
  client:load
  description="An attention-grabbing button with a continuous pulse animation, perfect for updates."
  code={`import { PulsatingButton } from '@/components/ui/pulsating-button';

export function Demo() {
  return <PulsatingButton>1 New Message</PulsatingButton>;
}`}
>
  <PulsatingButton client:load>1 New Message</PulsatingButton>
</ComponentPreview>

### Ripple Button

<ComponentPreview
  client:load
  description="A button with a smooth ripple effect on click, configured with custom ripple color."
  code={`import { RippleButton } from '@/components/ui/ripple-button';

export function Demo() {
  return <RippleButton rippleColor="#3498db">Click for Custom Ripple</RippleButton>;
}`}
>
  <RippleButton client:load rippleColor="#3498db">Click for Custom Ripple</RippleButton>
</ComponentPreview>

### Interactive Hover Button

<ComponentPreview
  client:load
  description="A lively button that morphs into an extended state with an arrow upon hovering."
  code={`import { InteractiveHoverButton } from '@/components/ui/interactive-hover-button';

export function Demo() {
  return <InteractiveHoverButton>Explore Features</InteractiveHoverButton>;
}`}
>
<InteractiveHoverButton client:load>Explore Features</InteractiveHoverButton>
</ComponentPreview>

## Components

Magic UI's marquee pattern is useful for testimonial walls, logo rails, announcement loops, and media strips that need continuous motion without feeling heavy.

Install with the official Magic UI command:

```bash
pnpm dlx shadcn@latest add @magicui/marquee
```

### Marquee

<MagicMarqueePreview client:load />

## Device Mocks

Magic UI also includes polished browser and handset shells for product screenshots, landing page demos, and animated app previews.

### Safari

<ComponentPreview
  client:load
  description="A Safari browser window mockup with support for image and video content."
  code={`import { Safari } from '@/components/ui/safari';

export function Demo() {
  return (
    <Safari
      className="w-full max-w-4xl"
      url="magicui.design"
      imageSrc="https://images.unsplash.com/photo-1518005020951-eccb494ad742?q=80&w=1600&auto=format&fit=crop"
    />
  );
}`}
>
  <Safari
    client:load
    className="w-full max-w-4xl"
    url="magicui.design"
    imageSrc="https://images.unsplash.com/photo-1518005020951-eccb494ad742?q=80&w=1600&auto=format&fit=crop"
  />
</ComponentPreview>

### iPhone

<ComponentPreview
  client:load
  description="An iPhone mockup for mobile screenshots and app preview imagery."
  code={`import { Iphone } from '@/components/ui/iphone';

export function Demo() {
  return (
    <Iphone
      className="w-full max-w-[260px]"
      src="https://images.unsplash.com/photo-1512941937669-90a1b58e7e9c?q=80&w=1200&auto=format&fit=crop"
    />
  );
}`}
>
  <Iphone
    client:load
    className="w-full max-w-[260px]"
    src="https://images.unsplash.com/photo-1512941937669-90a1b58e7e9c?q=80&w=1200&auto=format&fit=crop"
  />
</ComponentPreview>

### Android

<ComponentPreview
  client:load
  description="An Android device mockup for showcasing native or responsive mobile screens."
  code={`import { Android } from '@/components/ui/android';

export function Demo() {
  return (
    <Android
      className="w-full max-w-[250px]"
      src="https://images.unsplash.com/photo-1511707171634-5f897ff02aa9?q=80&w=1200&auto=format&fit=crop"
    />
  );
}`}
>
  <Android
    className="w-full max-w-[250px]"
    src="https://images.unsplash.com/photo-1511707171634-5f897ff02aa9?q=80&w=1200&auto=format&fit=crop"
  />
</ComponentPreview>

## Backgrounds

Magic UI ships a strong set of decorative background primitives for hero sections, section separators, and atmospheric content framing. The current catalog includes:

### Flickering Grid

<ComponentPreview
  client:load
  description="A flickering SVG grid background adapted from the official Magic UI Flickering Grid pattern."
  code={`import { FlickeringGrid } from '@/components/ui/flickering-grid';

export function Demo() {
  return (
    <div className="relative w-full overflow-hidden rounded-3xl border border-border bg-background">
      <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
        <div className="max-w-md text-center">
          <p className="text-xs font-semibold uppercase tracking-[0.3em] text-muted-foreground">
            Ambient Background
          </p>
          <h3 className="mt-3 text-3xl font-semibold tracking-tight text-foreground">
            Flickering Grid
          </h3>
          <p className="mt-3 text-sm text-muted-foreground">
            A subtle animated grid for hero sections and launch surfaces.
          </p>
        </div>
      </div>
      <FlickeringGrid
        className="absolute inset-0 z-0 h-full w-full"
        squareSize={4}
        gridGap={8}
        flickerChance={0.28}
        color="rgb(99, 102, 241)"
        maxOpacity={0.22}
      />
    </div>
  );
}`}
>
  <div className="relative w-full overflow-hidden rounded-3xl border border-border bg-background">
    <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
      <div className="max-w-md text-center">
        <p className="text-xs font-semibold uppercase tracking-[0.3em] text-muted-foreground">
          Ambient Background
        </p>
        <h3 className="mt-3 text-3xl font-semibold tracking-tight text-foreground">
          Flickering Grid
        </h3>
        <p className="mt-3 text-sm text-muted-foreground">
          A subtle animated grid for hero sections and launch surfaces.
        </p>
      </div>
    </div>
    <FlickeringGrid
      client:load
      className="absolute inset-0 z-0 h-full w-full"
      squareSize={4}
      gridGap={8}
      flickerChance={0.28}
      color="rgb(99, 102, 241)"
      maxOpacity={0.22}
    />
  </div>
</ComponentPreview>

The `FlickeringGrid` implementation follows the official Magic UI usage shape:

```tsx
import { FlickeringGrid } from '@/components/ui/flickering-grid';

<FlickeringGrid />
```

### Animated Grid Pattern

<ComponentPreview
  client:load
  description="Animated square cells and sweeping highlights for launch sections, hero canvases, and product marketing surfaces."
  code={`import { AnimatedGridPattern } from '@/components/ui/magic-backgrounds';

export function Demo() {
  return (
    <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-white">
      <AnimatedGridPattern className="opacity-90" />
      <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
        <div className="max-w-xl text-center">
          <p className="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">
            Launch Surface
          </p>
          <h3 className="mt-4 text-4xl font-semibold tracking-tight text-slate-950">
            Animated Grid Pattern
          </h3>
          <p className="mt-3 text-sm text-slate-600">
            Use it behind hero messaging when you want motion without heavy illustration.
          </p>
        </div>
      </div>
    </div>
  );
}`}
>
  <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-white">
    <AnimatedGridPattern className="opacity-90" />
    <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
      <div className="max-w-xl text-center">
        <p className="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">
          Launch Surface
        </p>
        <h3 className="mt-4 text-4xl font-semibold tracking-tight text-slate-950">
          Animated Grid Pattern
        </h3>
        <p className="mt-3 text-sm text-slate-600">
          Use it behind hero messaging when you want motion without heavy illustration.
        </p>
      </div>
    </div>
  </div>
</ComponentPreview>

### Retro Grid

<ComponentPreview
  client:load
  description="Perspective grid lines with a clean synthwave-style horizon treatment for bold splash screens and launch intros."
  code={`import { RetroGrid } from "@/registry/magicui/retro-grid";

export function Demo() {
  return (
    <div className="bg-background relative flex h-[500px] w-full flex-col items-center justify-center overflow-hidden rounded-lg border">
      <span className="pointer-events-none z-10 bg-linear-to-b from-[#ffd319] via-[#ff2975] to-[#8c1eff] bg-clip-text text-center text-7xl leading-none font-bold tracking-tighter whitespace-pre-wrap text-transparent">
        Retro Grid
      </span>
      <MagicUiRetroGrid />
    </div>
  );
}`}
>
  <div className="bg-background relative flex h-[500px] w-full flex-col items-center justify-center overflow-hidden rounded-lg border">
    <span className="pointer-events-none z-10 bg-linear-to-b from-[#ffd319] via-[#ff2975] to-[#8c1eff] bg-clip-text text-center text-7xl leading-none font-bold tracking-tighter whitespace-pre-wrap text-transparent">
      Retro Grid
    </span>
    <MagicUiRetroGrid />
  </div>
</ComponentPreview>

Install with the official Magic UI command:

```bash
pnpm dlx shadcn@latest add @magicui/retro-grid
```

Official usage:

```tsx
import { RetroGrid } from "@/registry/magicui/retro-grid";

export function RetroGridDemo() {
  return (
    <div className="bg-background relative flex h-[500px] w-full flex-col items-center justify-center overflow-hidden rounded-lg border">
      <span className="pointer-events-none z-10 bg-linear-to-b from-[#ffd319] via-[#ff2975] to-[#8c1eff] bg-clip-text text-center text-7xl leading-none font-bold tracking-tighter whitespace-pre-wrap text-transparent">
        Retro Grid
      </span>
      <RetroGrid />
    </div>
  );
}
```

### Ripple

<ComponentPreview
  client:load
  description="Expanding concentric waves that draw attention toward a centered CTA, stat, or announcement."
  code={`import { Ripple } from '@/components/ui/magic-backgrounds';

export function Demo() {
  return (
    <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-white">
      <Ripple />
      <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
        <div className="rounded-full border border-sky-200/70 bg-white/85 px-8 py-8 text-center shadow-[0_24px_80px_rgba(56,189,248,0.12)] backdrop-blur">
          <p className="text-xs font-semibold uppercase tracking-[0.35em] text-sky-600">
            Centered CTA
          </p>
          <h3 className="mt-3 text-3xl font-semibold tracking-tight text-slate-950">
            Ripple
          </h3>
        </div>
      </div>
    </div>
  );
}`}
>
  <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-white">
    <Ripple />
    <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
      <div className="rounded-full border border-sky-200/70 bg-white/85 px-8 py-8 text-center shadow-[0_24px_80px_rgba(56,189,248,0.12)] backdrop-blur">
        <p className="text-xs font-semibold uppercase tracking-[0.35em] text-sky-600">
          Centered CTA
        </p>
        <h3 className="mt-3 text-3xl font-semibold tracking-tight text-slate-950">
          Ripple
        </h3>
      </div>
    </div>
  </div>
</ComponentPreview>

### Dot Pattern

<ComponentPreview
  client:load
  description="A background dot pattern made with SVGs, fully customizable using Tailwind CSS."
  code={`import { DotPattern } from "@/registry/magicui/dot-pattern";

export function DotPatternDemo() {
  return (
    <div className="bg-background relative flex h-[500px] w-full flex-col items-center justify-center overflow-hidden rounded-lg border">
      <DotPattern className="text-muted-foreground/30 [mask-image:radial-gradient(300px_circle_at_center,white,transparent)]" />
    </div>
  );
}`}
>
  <div className="bg-background relative flex h-[500px] w-full flex-col items-center justify-center overflow-hidden rounded-lg border">
    <DotPattern className="text-muted-foreground/30 [mask-image:radial-gradient(300px_circle_at_center,white,transparent)]" />
  </div>
</ComponentPreview>

### Grid Pattern

<ComponentPreview
  client:load
  description="A clean technical grid with optional highlighted cells for blueprint motifs, editors, and developer-facing product surfaces."
  code={`import { GridPattern } from '@/components/ui/magic-backgrounds';

export function Demo() {
  return (
    <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-white">
      <GridPattern />
      <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
        <div className="max-w-lg text-center">
          <p className="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">
            Blueprint Surface
          </p>
          <h3 className="mt-4 text-4xl font-semibold tracking-tight text-slate-950">
            Grid Pattern
          </h3>
        </div>
      </div>
    </div>
  );
}`}
>
  <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-white">
    <GridPattern />
    <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
      <div className="max-w-lg text-center">
        <p className="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">
          Blueprint Surface
        </p>
        <h3 className="mt-4 text-4xl font-semibold tracking-tight text-slate-950">
          Grid Pattern
        </h3>
      </div>
    </div>
  </div>
</ComponentPreview>

### Hexagon Pattern

<ComponentPreview
  client:load
  description="A honeycomb-style field that feels more branded and stylized than a square grid, especially for dense hero backdrops."
  code={`import { HexagonPattern } from '@/components/ui/magic-backgrounds';

export function Demo() {
  return (
    <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-white">
      <HexagonPattern />
      <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
        <div className="rounded-[24px] border border-slate-200/80 bg-white/85 px-8 py-7 text-center backdrop-blur">
          <p className="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">
            Branded Geometry
          </p>
          <h3 className="mt-4 text-3xl font-semibold tracking-tight text-slate-950">
            Hexagon Pattern
          </h3>
        </div>
      </div>
    </div>
  );
}`}
>
  <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-white">
    <HexagonPattern />
    <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
      <div className="rounded-[24px] border border-slate-200/80 bg-white/85 px-8 py-7 text-center backdrop-blur">
        <p className="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">
          Branded Geometry
        </p>
        <h3 className="mt-4 text-3xl font-semibold tracking-tight text-slate-950">
          Hexagon Pattern
        </h3>
      </div>
    </div>
  </div>
</ComponentPreview>

### Striped Pattern

<ComponentPreview
  client:load
  description="Directional stripe layers that add momentum behind hero copy, badges, stats, and callout sections."
  code={`import { StripedPattern } from '@/components/ui/magic-backgrounds';

export function Demo() {
  return (
    <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-white">
      <StripedPattern />
      <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
        <div className="max-w-lg text-center">
          <p className="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">
            Momentum Layer
          </p>
          <h3 className="mt-4 text-4xl font-semibold tracking-tight text-slate-950">
            Striped Pattern
          </h3>
        </div>
      </div>
    </div>
  );
}`}
>
  <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-white">
    <StripedPattern />
    <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
      <div className="max-w-lg text-center">
        <p className="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">
          Momentum Layer
        </p>
        <h3 className="mt-4 text-4xl font-semibold tracking-tight text-slate-950">
          Striped Pattern
        </h3>
      </div>
    </div>
  </div>
</ComponentPreview>

### Interactive Grid Pattern

<ComponentPreview
  client:load
  description="A pointer-reactive grid that brightens around the cursor to add subtle motion to hero and showcase layouts."
  code={`import { InteractiveGridPattern } from '@/components/ui/magic-backgrounds';

export function Demo() {
  return (
    <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-white">
      <InteractiveGridPattern />
      <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
        <div className="rounded-[24px] border border-indigo-200/70 bg-white/80 px-8 py-7 text-center shadow-[0_20px_80px_rgba(99,102,241,0.12)] backdrop-blur">
          <p className="text-xs font-semibold uppercase tracking-[0.35em] text-indigo-600">
            Move Your Cursor
          </p>
          <h3 className="mt-4 text-3xl font-semibold tracking-tight text-slate-950">
            Interactive Grid Pattern
          </h3>
        </div>
      </div>
    </div>
  );
}`}
>
  <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-white">
    <InteractiveGridPattern />
    <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
      <div className="rounded-[24px] border border-indigo-200/70 bg-white/80 px-8 py-7 text-center shadow-[0_20px_80px_rgba(99,102,241,0.12)] backdrop-blur">
        <p className="text-xs font-semibold uppercase tracking-[0.35em] text-indigo-600">
          Move Your Cursor
        </p>
        <h3 className="mt-4 text-3xl font-semibold tracking-tight text-slate-950">
          Interactive Grid Pattern
        </h3>
      </div>
    </div>
  </div>
</ComponentPreview>

### Light Rays

<ComponentPreview
  client:load
  description="Soft volumetric light beams that sit well behind screenshots, centered headlines, and high-polish launch copy."
  code={`import { LightRays } from '@/components/ui/magic-backgrounds';

export function Demo() {
  return (
    <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-[linear-gradient(180deg,#ffffff_0%,#f5f7ff_100%)]">
      <LightRays />
      <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
        <div className="max-w-lg text-center">
          <p className="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">
            Atmosphere
          </p>
          <h3 className="mt-4 text-4xl font-semibold tracking-tight text-slate-950">
            Light Rays
          </h3>
          <p className="mt-3 text-sm text-slate-600">
            Useful behind product screenshots and spotlight messaging.
          </p>
        </div>
      </div>
    </div>
  );
}`}
>
  <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-[linear-gradient(180deg,#ffffff_0%,#f5f7ff_100%)]">
    <LightRays />
    <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
      <div className="max-w-lg text-center">
        <p className="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">
          Atmosphere
        </p>
        <h3 className="mt-4 text-4xl font-semibold tracking-tight text-slate-950">
          Light Rays
        </h3>
        <p className="mt-3 text-sm text-slate-600">
          Useful behind product screenshots and spotlight messaging.
        </p>
      </div>
    </div>
  </div>
</ComponentPreview>

### Noise Texture

<ComponentPreview
  client:load
  description="A fine-grain texture overlay that breaks up flat fills and makes polished surfaces feel less synthetic."
  code={`import { NoiseTexture } from '@/components/ui/magic-backgrounds';

export function Demo() {
  return (
    <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-[linear-gradient(135deg,#f8fafc_0%,#eef2ff_100%)]">
      <NoiseTexture />
      <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
        <div className="w-full max-w-lg rounded-[26px] border border-white/70 bg-white/70 p-8 shadow-[0_24px_80px_rgba(15,23,42,0.08)] backdrop-blur">
          <p className="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">
            Texture Layer
          </p>
          <h3 className="mt-4 text-3xl font-semibold tracking-tight text-slate-950">
            Noise Texture
          </h3>
          <p className="mt-3 text-sm text-slate-600">
            Blend it over flat fills to add warmth and visual richness.
          </p>
        </div>
      </div>
    </div>
  );
}`}
>
  <div className="relative w-full overflow-hidden rounded-[28px] border border-border bg-[linear-gradient(135deg,#f8fafc_0%,#eef2ff_100%)]">
    <NoiseTexture />
    <div className="relative z-10 flex min-h-[320px] items-center justify-center p-10">
      <div className="w-full max-w-lg rounded-[26px] border border-white/70 bg-white/70 p-8 shadow-[0_24px_80px_rgba(15,23,42,0.08)] backdrop-blur">
        <p className="text-xs font-semibold uppercase tracking-[0.35em] text-slate-500">
          Texture Layer
        </p>
        <h3 className="mt-4 text-3xl font-semibold tracking-tight text-slate-950">
          Noise Texture
        </h3>
        <p className="mt-3 text-sm text-slate-600">
          Blend it over flat fills to add warmth and visual richness.
        </p>
      </div>
    </div>
  </div>
</ComponentPreview>

### Background Catalog

[Official Magic UI Background Docs ↗](https://magicui.design/docs/components)

The live Magic UI catalog currently lists `Flickering Grid`, `Animated Grid Pattern`, `Retro Grid`, `Ripple`, `Dot Pattern`, `Grid Pattern`, `Hexagon Pattern`, `Striped Pattern`, `Interactive Grid Pattern`, `Light Rays`, and `Noise Texture` in the backgrounds group.

## Special Effects

Magic UI also includes high-contrast effect components for premium cards, motion accents, beams, and interactive highlights.

### Neon Gradient Card

<ComponentPreview
  client:load
  description="A beautiful neon card effect."
  code={`import { NeonGradientCard } from "@/registry/magicui/neon-gradient-card";

export function NeonGradientCardDemo() {
  return (
    <NeonGradientCard className="mx-auto flex h-56 w-[24rem] items-center justify-center text-center md:h-64 md:w-[28rem]">
      <div className="pointer-events-none relative z-10 flex -translate-y-4 flex-col items-center justify-center pb-6 leading-none">
        <span
          className="text-center text-4xl font-bold tracking-tighter text-transparent md:text-5xl"
          style={{
            backgroundImage: "linear-gradient(180deg, #ff2975 10%, #ff8aab 45%, #c4f5ff 100%)",
            WebkitBackgroundClip: "text",
            backgroundClip: "text",
          }}
        >
          Neon
        </span>
        <span
          className="mt-1 text-center text-4xl font-bold tracking-tighter text-transparent md:text-5xl"
          style={{
            backgroundImage: "linear-gradient(180deg, #ff2975 10%, #ff8aab 45%, #c4f5ff 100%)",
            WebkitBackgroundClip: "text",
            backgroundClip: "text",
          }}
        >
          Gradient
        </span>
        <div className="relative mt-1">
          <span
            className="block text-center text-4xl font-bold tracking-tighter text-transparent md:text-5xl"
            style={{
              backgroundImage: "linear-gradient(180deg, #ff8aab 10%, #d6efff 100%)",
              WebkitBackgroundClip: "text",
              backgroundClip: "text",
            }}
          >
            Card
          </span>
          <span
            aria-hidden="true"
            className="absolute inset-x-0 top-full -mt-1 block text-center text-4xl font-bold tracking-tighter text-transparent opacity-30 blur-[1px] md:text-5xl"
            style={{
              backgroundImage: "linear-gradient(180deg, #ff8aab 10%, #d6efff 100%)",
              WebkitBackgroundClip: "text",
              backgroundClip: "text",
              transform: "scaleY(-0.38)",
              transformOrigin: "top",
            }}
          >
            Card
          </span>
        </div>
      </div>
    </NeonGradientCard>
  );
}`}
>
  <NeonGradientCard client:load className="mx-auto flex h-56 w-[24rem] items-center justify-center text-center md:h-64 md:w-[28rem]">
    <div className="pointer-events-none relative z-10 flex -translate-y-4 flex-col items-center justify-center pb-6 leading-none">
      <span
        className="text-center text-4xl font-bold tracking-tighter text-transparent md:text-5xl"
        style={{
          backgroundImage: "linear-gradient(180deg, #ff2975 10%, #ff8aab 45%, #c4f5ff 100%)",
          WebkitBackgroundClip: "text",
          backgroundClip: "text",
        }}
      >
        Neon
      </span>
      <span
        className="mt-1 text-center text-4xl font-bold tracking-tighter text-transparent md:text-5xl"
        style={{
          backgroundImage: "linear-gradient(180deg, #ff2975 10%, #ff8aab 45%, #c4f5ff 100%)",
          WebkitBackgroundClip: "text",
          backgroundClip: "text",
        }}
      >
        Gradient
      </span>
      <div className="relative mt-1">
        <span
          className="block text-center text-4xl font-bold tracking-tighter text-transparent md:text-5xl"
          style={{
            backgroundImage: "linear-gradient(180deg, #ff8aab 10%, #d6efff 100%)",
            WebkitBackgroundClip: "text",
            backgroundClip: "text",
          }}
        >
          Card
        </span>
        <span
          aria-hidden="true"
          className="absolute inset-x-0 top-full -mt-1 block text-center text-4xl font-bold tracking-tighter text-transparent opacity-30 blur-[1px] md:text-5xl"
          style={{
            backgroundImage: "linear-gradient(180deg, #ff8aab 10%, #d6efff 100%)",
            WebkitBackgroundClip: "text",
            backgroundClip: "text",
            transform: "scaleY(-0.38)",
            transformOrigin: "top",
          }}
        >
          Card
        </span>
      </div>
    </div>
  </NeonGradientCard>
</ComponentPreview>

Install with the official Magic UI command:

```bash
pnpm dlx shadcn@latest add @magicui/neon-gradient-card
```

[Official Magic UI Special Effects Docs ↗](https://magicui.design/docs/components/neon-gradient-card)

## Text Animations

Magic UI also ships a broad set of animated typography components for hero copy, stats, product messaging, and editorial motion treatments.

### Text Animate

<MagicTextAnimatePreview client:load />

### Typing Animation

<MagicTypingAnimationPreview client:load />

### Line Shadow Text

<MagicLineShadowTextPreview client:load />

### Aurora Text

<MagicAuroraTextPreview client:load />

### Video Text

<MagicVideoTextPreview client:load />

### Number Ticker

<MagicNumberTickerPreview client:load />

### Shiny Text

<MagicAnimatedShinyTextPreview client:load />

### Gradient Text

<MagicAnimatedGradientTextPreview client:load />

### Text Reveal

<MagicTextRevealPreview client:load />

### Hyper Text

<MagicHyperTextPreview client:load />

### Word Rotate

<MagicWordRotatePreview client:load />

### Scroll Based Velocity

<MagicScrollBasedVelocityPreview client:load />

### Morphing Text

<MagicMorphingTextPreview client:load />

### Sparkles Text

<MagicSparklesTextPreview client:load />

### Spinning Text

<MagicSpinningTextPreview client:load />

### Text Highlighter

<MagicTextHighlighterPreview client:load />

### Text 3D Flip

<MagicText3DFlipPreview client:load />

## Installation

```bash
npx magicui add shimmer-button
```

Or copy the component source directly from the [Shimmer Button docs](/docs/magic-ui-shimmer-button).

## Design Philosophy

- **Motion with purpose** — Animations guide attention to key actions, not distract from them.
- **Performance** — Uses CSS animations and `transform-gpu` for hardware acceleration.
- **Theme-aware** — Components adapt to light/dark mode via CSS custom properties.
- **Composable** — Magic UI components accept `className` and `children` props for full customization.

## When to Use Magic UI

✅ **Use for**: Landing page CTAs, product tours, marketing hero sections, sign-up prompts.

❌ **Avoid for**: Dense data tables, admin dashboards, form-heavy workflows (use shadcn/ui instead).

## Related

- [Shimmer Button](/docs/magic-ui-shimmer-button) — Full component documentation
- [Buttons](/docs/buttons) — All button components compared
- [shadcn/ui](/docs/libraries/shadcn) — Functional primitives that complement Magic UI
